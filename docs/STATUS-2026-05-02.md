# CulinaSync — Status-Snapshot 2. Mai 2026 (vollständig)

> **Zweck:** Nachziehen aller dokumentierten Themen nach Sprint „M3.3 + Legacy-Settings + A11y + Gemini/Zod + CI Node 24“.  
> **Vorgänger:** [STATUS-2026-05-01.md](./STATUS-2026-05-01.md)

---

## Kurzfassung

| Bereich | Stand |
|--------|--------|
| **Architektur** | MealPlanner: `MealPlannerProvider`, `useMealPlannerContext`, `useMealPlannerScreen`; Konstanten `meal-planner/mealPlannerConstants.ts`; **DayColumn** / `PlannedMealCard`: Vorratskennzahl über **`getMealPlanSlotPantryStatus`** (`meal-planner/dayColumnPantryStatus.ts`). CookMode: `useCookModeController` + `cook-mode/*`. |
| **Settings** | Legacy `culinaSyncSettings` nur noch Migration → `persist:settings`; `loadSettings()` ohne Legacy-Lese-Fallback. Bootstrap: `store/migrateLegacySettingsBeforePersist.ts`. Module: `settingsKeys.ts`, `settingsMerge.ts`. |
| **Sicherheit / KI** | `geminiService.ts`: Zod (`parseAiJsonWithSchema`) für Ideen, Rezept, Einkaufsliste, Nährwert-Verifikation; API-`responseSchema` unverändert. |
| **A11y** | Header, RecipeDetail-Tabs (tablist/tabpanel), CookMode, Voice-Overlays (`role="status"`), Install-/PWA-Dialoge (`role="dialog"`), MealPlanner-Placement-Dismiss; neue i18n-Keys. |
| **CI / DevContainer** | `node-version: 24` in `validate.yml` und `ci.yml`; DevContainer-Image `typescript-node:24-bookworm`; `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true`. |
| **Build / Supply Chain** | TS2345-Fix `utilsCategories.test.ts`; npm/pnpm `overrides` für `serialize-javascript`, `uuid`; siehe [CHANGELOG.md](../CHANGELOG.md). |
| **Tests (M5-Fortschritt)** | **119** Vitest-Tests (**34** Dateien): oben genannte Suites plus `usePantryManager.test.tsx`, `ShoppingListContext.test.tsx`, Smoke `PantryManager` / `ShoppingList` (`smokeHookStubs.ts`), `mealPlanRepository.test.ts`, `pantryRepository.test.ts`, `dayColumnPantryStatus.test.ts`, `DayColumn.test.tsx`. `PantryManagerContext.test.tsx` aligned mit `pantryItems`/`setSearchTerm` (tsgo). Coverage v8 ca. **42 %** Statements / **44 %** Lines — Ziel ≥70 % offen. |
| **CI** | `validate.yml`: `test:coverage`, Artefakt **coverage-lcov**, Bundle-Budget bei jedem Lauf. |
| **Tooling** | `npm run check:all` (lint, type-check, test, build, budget, audit high); ESLint ignoriert `coverage/**`. |
| **Dokumentation** | README, ROADMAP v1.3, CHANGELOG, AUDIT, ARCHITECTURE, PROJECT-STRUCTURE, DEVELOPMENT, DEPLOYMENT, TESTING, docs/README, TROUBLESHOOTING, copilot-instructions, diese Datei. |

---

## Validierung (empfohlen)

```bash
pnpm run lint
pnpm run test
pnpm run type-check
pnpm run build
pnpm run check:bundle-budget
pnpm run check:all
pnpm run test:coverage
```

Ohne globales pnpm (z. B. Windows): `npm run lint`, `npm run test`, … oder `npx pnpm@10 install --frozen-lockfile` wie in CI.

---

## Roadmap-Bezug

- **M3.3** Essensplan-Context: erledigt.  
- **M4.1** Gemini-Validierung mit Zod: erledigt.  
- **M2.3** A11y/i18n: erweitert durch Sweep Mai 2026.  
- **M5** Coverage ≥70 %: weiterhin offen; Hooks/Smoke/Context und CI-Coverage-Artefakt sind umgesetzt (siehe ROADMAP M5 🟨).  
- Bekannte Offene Punkte: Legacy-Settings-Zeile in ROADMAP durch Migration ersetzt.

---

## Ergänzung (M5, gleicher Tag)

- **Pure Helper:** `getMealPlanSlotPantryStatus(recipe)` kapselt die Statuslogik (`unknown` / `ok` / `partial` / `missing`) für Essensplan-Slots — einfach testbar, keine Dexie-Abhängigkeit.
- **CI:** Unverändert `validate.yml` (Lint → `test:coverage` → Build → Bundle-Budget); vor Push **`npm run check:all`** oder pnpm-Äquivalent empfohlen.

---

## Verwandte Dokumente

- [ROADMAP.md](../ROADMAP.md) — Version 1.3  
- [AUDIT.md](../AUDIT.md) — Status-Block 2026-05-02  
- [CHANGELOG.md](../CHANGELOG.md) — [Unreleased]
