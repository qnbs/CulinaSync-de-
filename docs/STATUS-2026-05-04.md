# CulinaSync — Status-Snapshot 4. Mai 2026

> **Vorgaenger:** [STATUS-2026-05-02.md](./STATUS-2026-05-02.md)

## Kurzfassung

| Bereich | Stand |
|--------|--------|
| **Tests** | Vitest **222** Tests in **59** Dateien (u. a. Einkaufsliste **`BulkAddModal`** / **`AiModal`**, **`PantryList`**; Hooks: Debounce, WindowSize, SpeechSynthesis, SpeechRecognition-Shim, WakeLock, ModalA11y; Services: retryUtils, mealPlannerSmartService, utils; `useRecipeDetail`; Smoke: App inkl. `services/db`-Mock, RecipeCard, GlobalErrorBoundary; erweiterte `voiceCommands`). |
| **Coverage (v8)** | ca. **59 %** Statements / **61 %** Lines / **46 %** Branches / **52 %** Functions — Ziel ROADMAP **≥70 %** weiterhin **offen**; Vitest **thresholds** (Lines **60**, Statements **58**, Branches **45**, Functions **51**) gegen Rueckschritt. |
| **Test-Infra** | `fake-indexeddb` + Setup-Shims (IndexedDB, Speech APIs); `vitest.config`: `__APP_VERSION__`, `testTimeout` 30s, `maxWorkers: 1`; ESLint `react-hooks/exhaustive-deps`: **warn**, Fixes in `usePantryManager`. |
| **Bundle (M9.3)** | Zusaetzlicher Chunk **`vendor-export`** (jspdf, html2canvas, papaparse); Workbox `globIgnores` / `runtimeCaching` / `modulePreload` angepasst. |
| **Tauri (M8)** | `package.identifier` in `src-tauri/tauri.conf.json`; Workflow **`tauri-release.yml`** (Prep: Web-Build + Config-Check). Vollstaendiges Crate folgt bei Cargo-Workspace. |
| **QA** | Manuelle Live-Demo-Checkliste: [LIVE-DEMO-QA.md](./LIVE-DEMO-QA.md). |

## Validierung

```bash
npm run check:all
npm run test:coverage
```

## Roadmap

- **M5** Coverage ≥70 %: fortgesetzt, nicht abgeschlossen.
- **M9.3** vendor-misc: Teilmasznahme durch `vendor-export`.
- **M8** Desktop: Prep-Workflow + Identifier; Release-Matrix bei Cargo-Ergaenzung.
