# Status-Snapshot 2026-06-03 (Audit-Nachzug)

> **Letzte Aktualisierung:** 2026-06-03 — CI Deploy/E2E grün (#61); Post-Deploy-Verify + Vercel Protection dokumentiert.

## Executive Summary

CulinaSync-de- bleibt **produktionsreif** als Local-First-PWA. Der externe Audit (3. Juni 2026) nennt teils veraltete Coverage-Zahlen (~59–61 %) — **aktuell** liegen alle vier PRD-Metriken **über** dem M5-Ziel (siehe unten). Die automatisierten i18n-Gates melden **Baseline 0** und **0** Hardcoded-Funde im Production-Scan.

## Phase 0 — i18n (3. Juni 2026)

| Check | Ergebnis |
|-------|----------|
| `pnpm run i18n:scan` | **0** Kandidaten |
| `pnpm run i18n:check` | de/en Parität OK, Baseline **0** |
| `pnpm run i18n:check:changed` | OK |
| `i18n.ts` | `fallbackLng: ['de','en']`, `parseMissingKeyHandler` (Dev-Warnung, Prod leerer Fallback) |

**Hinweis:** Sichtbare Missing-Key-Placeholder in der UI sollten mit obigen Gates nicht mehr auftreten. Neue Features (z. B. Nextcloud-Sync) erhalten Pflicht-Keys in `de` + `en`.

## Validierung (Branch-Stand)

| Check | Ergebnis |
|-------|----------|
| `pnpm run test` | **405+** Tests (Vitest + `test:scripts`) |
| `pnpm run test:coverage` | **~78 %** stmts / **~79 %** lines / **~73 %** funcs / **~63 %** branches |
| Thresholds `vitest.config.ts` | **77/79/72/62** — grün |
| `pnpm run type-check` | tsgo |
| `pnpm run i18n:check` | grün |
| `cargo check` (`src-tauri`) | Tauri-2-Workspace |

## Neu in diesem Nachzug

| Thema | Status |
|-------|--------|
| **Local AI (Prefs)** | `LocalAiPanel`, `aiService` routing, `settings.localAi` + [LOCAL-AI-ARCHITECTURE.md](./LOCAL-AI-ARCHITECTURE.md) |
| **Help / Settings UX** | 12 FAQs, Pro-Tipps, `SettingsPanelIntro`, Einstellungs-Deep-Links |
| **i18n** | `aiChef.json` (UI-Strings KI-Chef getrennt von `settings.aiChef`) |
| **M10.2** Nextcloud | `nextcloudSyncAdapter`, `syncTransport`, Provider-UI in DataPanel, i18n-Fehlercodes |
| **M8** Tauri 2 | `Cargo.toml`, `src/`, `capabilities/`, Icons-Script, `tauri-release.yml` Matrix |
| **M7** TS 7 GA | `pnpm run check:ts-ga` — Upgrade erst bei npm stable ≥ 7.0.0 |
| **Audit-Doku** | Dieser STATUS, `instructions.md` Coverage-Korrektur |

## Deploy (Juni 2026)

| Kanal | Stand |
|-------|--------|
| **Vercel Production** | `apps/web/vercel.json`; Dashboard-Root `apps/web`; `pnpm run verify:deploy` |
| **GitHub Pages** | `deploy.yml` + `verify-pages-artifact`; Post-Deploy-Smoke mit `checkout` im Deploy-Job; SPA-`404.html` aus `public/` |
| **Deploy Health** | Workflow `deploy-health.yml` (täglich + manuell) |
| **CI** | E2E: Seite laden → offline → Banner; Vercel Monorepo-Build `--filter=web`; Playwright **v1.60.0** |

## Audit vNext (3. Juni 2026)

- Vollständiger Report: [`AUDIT-vNEXT-2026-06-03.md`](./AUDIT-vNEXT-2026-06-03.md) — **9,0/10**
- Backlog: [`AUDIT-REMEDIATION-BACKLOG.md`](./AUDIT-REMEDIATION-BACKLOG.md)
- Sofort umgesetzt: Device-Sync Zod-Gate; `exportService` → `dbInstance`; PR-Review-Regel `.cursor/rules/300-pr-review-automation.mdc`

## Offen / nächste Schritte

1. **M7** — TypeScript 7.0 GA auf npm abwarten, dann Prep-Doc abarbeiten.
2. **M8** — Ersten Tag-Release (`v*`) auslösen und Matrix-Artefakte prüfen; Deep-Link-Plugin optional.
3. **M5 Wartung** — Schwache Bereiche: `recipeRepository` branches, `voiceCommands`, `pantryCategoryLabels`.
4. **Optional** — Client-Side Encryption für Dexie (Phase 3 des Audits).

## Referenzen

- [`STATUS-2026-06-02.md`](STATUS-2026-06-02.md)
- [`M7-TYPESCRIPT-7-GA-PREP.md`](M7-TYPESCRIPT-7-GA-PREP.md)
- [`M8-TAURI-DESKTOP.md`](M8-TAURI-DESKTOP.md)
- [`ROADMAP.md`](../ROADMAP.md)
