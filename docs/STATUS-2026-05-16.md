# Status-Snapshot 2026-05-16

## Executive Summary

Nach der **Monorepo-Migration** (Turborepo + pnpm: `apps/web`, `packages/ai-core`, `packages/ui`) wurde ein **vollständiger Re-Audit** und **Housekeeping-Lauf** durchgeführt: Supply-Chain bereinigt, CI um `pnpm audit` erweitert, Kanon-Doku und Agent-Regeln auf die neue Struktur ausgerichtet.

## Validierung

| Check | Ergebnis |
|-------|----------|
| `pnpm run lint` | grün |
| `pnpm run type-check` | grün |
| `pnpm run test` | grün (218 Tests; Vitest `singleFork` unter Windows) |
| `pnpm run build` | grün |
| `pnpm run check:bundle-budget` | grün (~201 KB total) |
| `pnpm audit --audit-level=high` | **0** (nach Overrides; vorher 19 Findings) |
| `pnpm run i18n:scan` | Report in `reports/i18n-hardcoded-report.md` (730 potenzielle Treffer — historisch, kein Blocker) |

**Node:** CI/DevContainer **24** (`.node-version`). Lokale Runs können auf Node 22 mit Engine-Warnung laufen.

## Supply-Chain (behoben)

Root-`pnpm.overrides` in [`package.json`](../package.json):

- `protobufjs` → `^7.5.6`
- `@babel/plugin-transform-modules-systemjs` → `^7.29.4`
- `fast-uri` → `^3.1.2`

## CI / Infra

- [`validate.yml`](../.github/workflows/validate.yml): Schritt **Dependency audit (high+)** nach Bundle-Budget.
- [`lighthouserc.json`](../lighthouserc.json): `staticDistDir: ./apps/web/dist`.
- `@domain/ui#build`: erzeugt `dist/` für Turbo-Cache (kein leerer Build mehr).

## Architektur-Stichprobe (Migration)

Neue/verschobene Schichten in `apps/web/src/store/`: `indexedPersistStorage.ts`, `stateDexie.ts`, `transientUiStore.ts`, `aiCloudApi.ts`; `snapshotVaultService.ts` — Grenzen unverändert: Dexie über `db.ts`/Repositories, Gemini nur `geminiService.ts`.

## Offen (bewusst nicht in diesem Lauf)

- **M5 Coverage ≥70 %** — weiterhin ~59 % / 61 % (siehe [`ROADMAP.md`](../ROADMAP.md)).
- **M8 Tauri Release-Matrix**, **M10 Multi-Sync**.
- Doppelter Validate-Lauf auf `main` (`ci.yml` + `deploy.yml`) — dokumentiert, keine Entkopplung in diesem PR.

## Referenzen

- Migration: Commit `92396b1`
- Housekeeping: siehe `[Unreleased]` in [`CHANGELOG.md`](../CHANGELOG.md)
- Vollaudit-Historie: [`AUDIT.md`](../AUDIT.md)
