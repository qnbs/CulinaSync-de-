# Status-Snapshot 2026-06-02 (M5 abgeschlossen)

> **Letzte Aktualisierung:** Nach Merge von PR #20, #29, #30 und Docs-Audit-Nachzug.

## Executive Summary

**Milestone 5 (Testing) ist abgeschlossen:** alle vier PRD-Coverage-Metriken erreicht (Statements/Lines/Functions ≥70 %, Branches ≥60 %). CulinaSync-de- bleibt **produktionsreif** für privaten Local-First-Einsatz — starke Architektur, Privacy, CI. Keine kritischen App-Sicherheitslücken auf `main`.

## Validierung (`main`)

| Check | Ergebnis |
|-------|----------|
| `pnpm run lint` | grün |
| `pnpm run type-check` | grün (tsgo) |
| `pnpm run test` | grün — **362** Tests, **85** Dateien |
| `pnpm run test:coverage` | grün — Thresholds **77/79/72/62** |
| Coverage (v8) | **~78 %** stmts / **~79 %** lines / **~63 %** branches / **~72,5 %** funcs |
| `pnpm run build` | grün |
| `pnpm run check:bundle-budget` | grün |
| `pnpm audit --audit-level=high` | grün (Vitest **4.1.8** via `pnpm.overrides`, `@vitest/coverage-v8` **4.1.8**, Storybook 10) |

## Merge-Stand (Juni 2026)

| PR / Thema | Status |
|------------|--------|
| **#25** | Audit, erste M5-Tests, CI-Deduplikation, Vitest 4.1+ |
| **#26–#27** | M5 Coverage-Sprint + Follow-up (Services, Thresholds) |
| **#28** | UI-Smoke (Branches ≥60 %) |
| **#29** | Functions ≥70 %, Merge-Konflikte `main`, TS-Fixes in Smoke-Tests |
| **#22** | Storybook 10 (Major) |
| **#20** | Testing-Deps: vitest/jsdom/msw 4.1.8 / 29 / 2.14 |
| **#30** | Linting-Deps: eslint 10.4.1, typescript-eslint 8.60.1 (Ersatz #21) |
| **#21** | geschlossen — Inhalt in #30 |
| **E2E** | `.github/workflows/e2e-smoke.yml` (wöchentlich + `workflow_dispatch`) |

## Was nach M5 noch offen ist

| Priorität | Milestone / Thema | Kurzbeschreibung |
|-----------|-------------------|------------------|
| **P1** | M5 Wartung | Coverage >75 % halten; `db.ts` isoliert ungetestet (Import-Zyklen) |
| **P1** | i18n-Gate | `i18n:check:changed` verschärfen; verbleibende Hardcoded-Kandidaten |
| **P1** | PWA Offline-UX | Workbox-Strategien, Offline-Indikatoren (PRD P1) |
| **P2** | M7 | TypeScript 7.0 GA + `noUncheckedSideEffectImports`, `stableTypeOrdering` |
| **P2** | M8 | Tauri Desktop-Release (Matrix-Builds) |
| **P2** | M9.3 | Rest-`vendor-misc`-Chunk weiter splitten |
| **P2** | Hooks | `react-hooks/exhaustive-deps` von `warn` → schrittweise `error` |
| **P3** | M10 | Multi-Device-Sync (WebDAV, QR, Konflikte) |
| **Optional** | E2E ausbauen | Playwright-Smoke erweitern (separates Workflow) |
| **Optional** | `@hookform/resolvers` 5.x | Dependabot #24 — Major prüfen |

## Nächste sinnvolle Schritte (Reihenfolge)

1. **Doku & Agenten-Kanon** — dieser Nachzug; `copilot-instructions`, `TESTING.md`, `README` auf `main`-Stand.
2. **Release-Hygiene** — `CHANGELOG` [Unreleased] bei nächstem Tag bumpen; optional Patch **0.2.2**.
3. **P1 i18n / PWA** — kleine, nutzer sichtbare Verbesserungen ohne Architektur-Bruch.
4. **M7** — sobald TS 7.0 stable (kein Zeitdruck).
5. **M8 Tauri** — wenn Desktop-Distribution Priorität hat.

## CI / Infra

- [`validate.yml`](../.github/workflows/validate.yml) — lint, type-check, coverage, build, budget, audit
- [`ci.yml`](../.github/workflows/ci.yml) — PR: validate + i18n-changed
- [`deploy.yml`](../.github/workflows/deploy.yml) — `main`: validate + GitHub Pages
- Validate auf `main` primär über Deploy (kein Doppel-Lauf mit `ci.yml`)

## Referenzen

- Audit (Phase 0): [`docs/AUDIT-REPORT-2026-06.md`](AUDIT-REPORT-2026-06.md) — historischer Snapshot; M5-Nachzug siehe oben
- Roadmap: [`ROADMAP.md`](../ROADMAP.md)
- Testing: [`TESTING.md`](TESTING.md)
- Vorgänger: [`STATUS-2026-05-16.md`](STATUS-2026-05-16.md)
